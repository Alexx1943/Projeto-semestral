spring:
  application:
    name: projeto-semestral

  datasource:
    url: jdbc:mysql://localhost:3306/projetoSemestral
    username: alex
    password: 1234

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  threads:
    virtual:
      enabled: true


server:
  port: 8080
  servlet:
    context-path: /
  error:
    include-stacktrace: ON_PARAM
  tomcat:
    threads:
      max: 20